# MindBridge 30 天代码改造与每日推送计划

开始日期：2026-09-15。计划连续执行 30 天，至 2026-10-14。每天以一个可提交的代码切片为目标，并推送到 `git@github.com:cccniuniu-hue/Agent.git` 的 `main` 分支。

## 执行标准

- 每天从当天的任务开始；若前一天未完成，先补齐最早未完成项，再尽力推进当天任务。不可把未实现的功能标记为完成。
- 实现相应功能并检查语法与构建：Java 改动至少执行 Maven `compile`；Python 改动至少执行语法编译；配置文件检查其格式。无需启动 Docker、Ollama、Marker、Chroma、Qwen2-VL 或 reranker，也不把未运行的服务描述成“已跑通”。
- 当天提交包含代码、必要的配置或文档，以及本文件的进度标记。只暂存相关文件，提交前检查差异与敏感信息；不提交 API Key、真实用户对话、模型权重、上传文件或生成的数据。
- 提交后执行 `git push origin main`，再用远程 SHA 验证推送结果。若远程先有新提交，只做安全的 fetch/rebase；遇到冲突、权限或网络问题时保留现场并报告，不强推、不虚报成功。
- 运行前先检查工作区。若有用户未提交的改动，不能混入当天提交；可在独立分支安全完成当天代码，但须明确报告是否已推送及是否进入 `main`。

## 每日任务

- [ ] D01（09-15）：添加不依赖模型服务的 Java 编译检查与 Python 语法检查流程，并补充忽略规则，确保后续每天有统一的静态验收入口。
- [ ] D02（09-16）：把知识库 Chroma 网关调整为与固定 Chroma 版本匹配的集合创建、写入、查询和删除接口。
- [ ] D03（09-17）：把用户画像 Chroma 网关迁移到相同接口，并保留按用户 ID 过滤的隔离约束。
- [ ] D04（09-18）：为 OpenAI `text-embedding-3-small` 增加可配置的 512 维参数和响应维度校验。
- [ ] D05（09-19）：让知识库 Chroma 写入和查询使用同一 embedding 客户端生成的向量，记录模型与维度版本。
- [ ] D06（09-20）：为用户画像提供独立的 embedding 配置和客户端接口，避免默认把敏感画像文本送到外部 API。
- [ ] D07（09-21）：实现知识与画像向量索引的可重复重建命令，并保留数据库主数据和失败回退。
- [ ] D08（09-22）：抽象文档解析结果，统一记录正文、来源、页码、标题、图片清单与解析状态。
- [ ] D09（09-23）：新增 Marker Python HTTP 服务，支持 PDF 转 Markdown 和图片清单输出。
- [ ] D10（09-24）：为 Marker 服务补 DOCX 文件解析、输入限制、错误返回与超时契约。
- [ ] D11（09-25）：Java 上传链路接入 Marker HTTP 客户端，保留现有纯文本解析的降级路径。
- [ ] D12（09-26）：通过 commonmark-java 解析 Markdown AST，提取标题层级、段落、表格和图片引用。
- [ ] D13（09-27）：实现按章节和段落递归切块，超长段落再按 token 上限切分，并保留来源元数据。
- [ ] D14（09-28）：处理表格与代码块的切块边界；超长表格按行拆分并重复表头，避免超过 embedding 限制。
- [ ] D15（09-29）：新增 Qwen2-VL 图片描述 HTTP 服务接口和结构化输出，使用模拟客户端完成语法与契约检查。
- [ ] D16（09-30）：Java 端接入图片描述服务，补齐图片路径关联、超时与失败时跳过图片的行为。
- [ ] D17（10-01）：把图片描述回填到 Markdown 原位置，并生成带原图来源的独立可检索 chunk。
- [ ] D18（10-02）：将向量和 BM25 的粗召回候选数调整为可配置的 Top50，并保持降级检索。
- [ ] D19（10-03）：实现 RRF 融合并保留现有加权融合为可选策略，支持按数据评测选择。
- [ ] D20（10-04）：新增 `BAAI/bge-reranker-v2-m3` Python HTTP 服务，定义批量评分请求、响应和错误格式。
- [ ] D21（10-05）：Java 检索链路接入 BGE reranker，形成 Top50→Top5；服务失败时退回初排 Top5。
- [ ] D22（10-06）：改进 query 改写：保留原始问题，仅对复杂咨询启用改写，并限制输出与耗时。
- [ ] D23（10-07）：改进知识充分性判断和至多一次二次召回，合并去重原始与改写结果。
- [ ] D24（10-08）：补 Agent loop 状态路由约束、最大步数与 trace 记录，确保风险处理顺序不被跳过。
- [ ] D25（10-09）：将短期对话摘要改为阶段性更新，保持 Redis 读取与 MySQL 回退。
- [ ] D26（10-10）：将长期画像抽取改为会话结束、约十轮对话或重要状态变化触发，并实现去重合并。
- [ ] D27（10-11）：补画像删除、Chroma 索引同步删除、跨用户隔离及脱敏处理。
- [ ] D28（10-12）：让知识型回答携带可核对的来源；检索证据不足时明确说明，并限制文档中的提示注入。
- [ ] D29（10-13）：完善现有 RAGAS 脚本与样例，记录忠实度、答案相关性、上下文精确率和召回率的离线评估入口。
- [ ] D30（10-14）：完成代码与配置的最终编译/语法检查，更新 README、架构与降级说明，汇总 30 天提交并推送。

## 进度记录

每天完成后，在对应任务行标记 `[x]`；当日运行报告记录日期、提交 SHA、推送到的远程 SHA、语法/编译结果及任何未验证的运行条件。若任务未完成，保留 `[ ]`，明确写出阻塞原因。
